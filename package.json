{
  "name": "@safeheron/snap-offline-recovery-tool",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:algo": "yarn workspace @safeheron/mm-key-recovery build",
    "build:ui": "yarn workspace @safeheron/snap-offline-recovery-tool-ui build",
    "dev:ui": "yarn workspace @safeheron/snap-offline-recovery-tool-ui dev",
    "dev": "yarn build:algo && yarn run dev:ui",
    "build": "yarn workspaces run build",
    "test": "yarn workspace @safeheron/mm-key-recovery test"
  },
  "description": "snap-offline-recovery-tool",
  "workspaces": {
    "packages": ["packages/*"]
  },
  "repository": "https://github.com:Safeheron/snap-offline-recovery-tool.git"
}
